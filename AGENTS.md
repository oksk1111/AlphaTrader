# AGENTS.md — 개발 지침서

## v6.1 (2026-09-10) — 계기판을 먼저 고친다: "관측되지 않는 고장은 고쳐지지 않는다"

### 무슨 일이 있었나

v6.0 을 9월 8일에 배포했다. 5대 사고 원인을 모두 고쳤고, CI 검증 루프도 붙였고,
전 국면을 통과했다. 그런데 9월 10일에도 **거래는 여전히 0건**이었다.

사용자가 대시보드에서 복사해 준 프로덕션 로그가 결정적이었다:

```
2026-09-10 23:14:18 INFO ✅ KR Account Cache Updated: deposit=2715678, holdings=0
2026-09-10 23:14:17 INFO ✅ US Account Cache Updated: deposit=$330.64, holdings=0
2026-09-10 23:08:18 INFO ✅ KR Account Cache Updated: deposit=2715678, holdings=0
...  (약 6분 간격으로 40분간 이것만 반복)
```

이 40분(22:34~23:14 KST)은 **미국 정규장이 열려 있던 시간**이다(서머타임 기준
22:30 개장). 그런데 봇의 매매 로그가 한 줄도 없다. 동시에 대시보드는
`bot_status: running`, `market_status: CLOSED` 를 표시하고 있었다.

**저 로그는 봇이 쓴 게 아니었다. 대시보드가 자기 계좌 캐시를 5분마다 갱신하며 쓴
것이다** (`web/app.py: CACHE_MAX_AGE_SECONDS = 300`). 봇의 heartbeat 는 1분 주기인데
그 줄이 하나도 없다는 사실이 그것을 증명한다.

### 진짜 원인: 계기판 세 개가 동시에 거짓말을 하고 있었다

| # | 거짓말 | 코드 | 사용자가 본 것 | 실제 |
|---|--------|------|----------------|------|
| ① | **로그 화면이 봇이 쓰지 않는 파일을 보여줌** | `logging.FileHandler(f"trading_{today}.log")` — 프로세스 시작 시 파일명 확정, 날짜 회전 없음. 대시보드는 `sorted(glob("trading_*.log"))[-1]` 로 "가장 최근 날짜" 파일을 추측 | 계좌 캐시 로그만 반복 | 9/8 에 뜬 봇은 계속 `trading_20260908.log` 에 쓰고, 나중에 재시작된 대시보드가 만든 `trading_20260910.log` 가 화면에 뜸 |
| ② | **시장 상태가 서머타임을 모름** | `web/app.py:get_market_status()` 가 US 를 `23:30~06:00 KST` 로 하드코딩. v6.0 은 `run_bot` 의 시계만 `market_clock` 으로 옮기고 이 함수를 놓침 | 22:30~23:30 에 `CLOSED` | 미국장 개장 중 |
| ③ | **"프로세스가 있다"를 "살아 있다"로 표시** | `get_bot_pid()` = `pgrep`. 진행 여부는 보지 않음. `auto_restart_bot.sh` 도 동일 | `🟢 Running` | 44분간 아무 진행 없음 |

그리고 계기판 뒤에는 실제 고장이 있었다:

| # | 고장 | 영향 |
|---|------|------|
| ④ | **소액 계좌에서 매수 수량이 구조적으로 0** — `per_ticker_cash = $330.64 / 12종목 = $27.55` (어떤 종목도 1주 미만). 게다가 호출부가 `int(qty) * size_multiplier` 로 **정수화 뒤에** 배율을 곱함 → `int(1 × 0.5) = 0` | 배율이 1.0 이 아닌 모든 US 매수가 조용히 증발 |
| ⑤ | **감시 스크립트가 서머타임을 모름** — `auto_restart_bot.sh:is_market_hours()` 가 US 를 `23:20~06:10 KST` 로 하드코딩. 게다가 `check_and_restart_bot` 이 이 창 **안에서만** 호출됨 | 서머타임 기간 개장 첫 50분간 봇이 죽어 있어도 아무도 되살리지 않음 |
| ⑥ | **자가진단이 `job()` 안에 있음** — `health_monitor.diagnose()` 는 세션이 끝날 때 돈다 | `job()` 자체가 안 도는 최악의 고장이 정확히 사각지대 |

### 이 프로젝트가 v3→v6 까지 네 번 실패한 진짜 이유

전략을 고친 게 아니다. **계기판이 고장난 채로 계속 엔진만 뜯어고쳤다.**
무엇이 고장났는지 볼 수 없으니 매번 추측으로 "전면 개편"을 했고, 매번 다른 곳을
고쳤고, 매번 증상이 남았다.

그래서 **v6.1 은 전략보다 관측을 먼저 고친다.** 이것이 이 버전의 유일한 원칙이다.

> **관측되지 않는 고장은 고쳐지지 않는다.**
> 화면이 사실과 다르면, 그 뒤의 모든 판단은 추측이다.

### v6.1 규칙 (반드시 지킬 것)

1. **시계는 한 곳에만 있다.** 시장 시각이 필요하면 무조건 `modules/market_clock`.
   `run_bot`, `web/app.py`, `auto_restart_bot.sh` 가 각자 시간표를 갖는 순간
   반드시 어긋나고, 어긋난 쪽이 화면이면 사용자가 오진한다.
   → `tests/test_v61_observability.py::TestSingleClock` 이 잠근다.

2. **로그 파일의 주인은 이름으로 못박는다.** 봇은 `database/trading.log`,
   대시보드는 `database/dashboard.log`. 날짜 회전은 `TimedRotatingFileHandler` 가
   한다. 화면은 "가장 최근 파일"을 **추측하지 않고** `modules.logger.BOT_LOG_FILE`
   을 직접 읽는다. 로그 줄에는 `[bot]` / `[dash]` 가 붙는다.

3. **생존은 존재가 아니라 진행으로 판정한다.** 봇은 `database/heartbeat.json` 을
   주기적으로 갱신한다. `auto_restart_bot.sh` 와 대시보드는 그 파일의 **나이**를
   본다. 장중 5분(장외 30분) 이상 멈춰 있으면 프로세스가 있어도 좀비로 보고
   강제 재기동한다. 대시보드는 `🟠 Stalled` 를 표시한다 — `🟢 Running` 이라고
   쓰지 않는다.

4. **감시는 감시 대상 바깥에 둔다.** `job()` 이 안 도는 고장은 `job()` 안의
   진단으로 잡을 수 없다. 메인 루프의 `_check_session_stall()` 이
   "장이 열린 지 10분이 지났는데 이 세션에 `job()` 진입 기록이 없다"를 감시한다.

5. **감시 스크립트는 봇을 항상 살려둔다.** "장중에만 되살린다"는 설계는 틀렸다.
   개장 순간에 프로세스가 있을 보장이 사라진다. `job()` 은 CLOSED 면 즉시
   반환하며 KIS 토큰도 발급하지 않으므로 장 밖에서 살려둘 비용이 없다.

6. **`buy` 판정은 반드시 1주 이상으로 이어진다.** 현금이 1주를 감당하는데 0주를
   내면 판단 계층이 거짓말을 하는 것이다. 사이즈 배율은 **정수화 전에 금액에**
   곱한다. 과매수는 수량 0 이 아니라 `MAX_POSITION_PCT`(종목 노출 한도)로 막는다.

7. **자금은 후보 수가 아니라 채울 수 있는 슬롯 수로 나눈다.** 최소 주문금액보다
   잘게 쪼개면 분산이 아니라 전면 정지다.

8. **검증 루프는 운영 설정을 읽는다.** `verify_strategy_loop.py` 가 손절/트레일링
   값을 스크립트에 복사해 두면, 운영 설정을 바꿔도 검증은 옛날 값으로 통과한다.
   이제 `user_config.json` 에서 읽는다.

### 감시 장치를 넣으면서 만든 자책골 두 개 (실서버에서 잡았다)

관측 장치를 도입하는 작업 자체가 새로운 고장 모드를 만든다. 두 개를 실서버
관측으로 잡았고, 둘 다 회귀 테스트로 잠갔다.

**(a) 감시가 정상 동작 중인 봇을 죽일 뻔했다.**
`job()` 은 세션이 끝날 때까지(최대 6.5시간) 메인 루프를 점유한다. 그동안
`schedule.run_pending()` 도 메인 루프의 `touch_heartbeat` 도 돌지 않으므로,
**정상적으로 매매 중인 봇의 heartbeat 가 세션 내내 멈춰 있다.** 좀비 감시가
그것을 보고 개장 5분 뒤 `pkill -9` 를 날린다 — 고치려던 문제("멈춘 봇을 살아
있다고 표시")의 정확한 반대 방향 오류다.

그래서 `job()` 안에서 5분 이상 침묵할 수 있는 구간을 전부 덮었다:

```
job-enter → session-prep → scan → sentiment → evaluate → watchloop
```

실서버에서 `session-prep` 이 60초 넘게 유지되는 것을 보고 `scan`/`sentiment` 를
추가했다 — 종목당 REST 2~3회(후보 12~21개) + LLM 왕복이 그 구간에 들어간다.

> **규칙: 좀비 감시를 넣을 때는 "감시 대상이 정상적으로 오래 걸리는 구간"을
> 먼저 나열하라.** 그 목록이 곧 heartbeat 를 찍어야 할 지점이다.

**(b) 봇이 두 개 돌 뻔했다.**
`deploy.sh` 는 `pkill` 후 3초 뒤에 봇을 새로 띄운다. v6.1 이 감시 스크립트를
"장 안팎 없이 항상" 돌게 바꾸면서, 그 3초 안에 감시가 "봇이 없다"고 판단해 또
하나를 띄울 확률이 크게 올라갔다. **봇이 두 개면 같은 신호에 주문이 두 번 나간다.**

- `deploy.sh` 가 `database/.deploying` 을 만들고 `trap EXIT` 으로 해제
- 감시는 락이 있는 동안 재기동/좀비판정을 건너뛴다 (락이 5분 넘게 남아 있으면
  배포가 죽은 것으로 보고 무시 — 영구 정지 방지)
- 그래도 두 개 이상이면 `kill_duplicate_bots()` 가 가장 오래 살아 있는 하나만
  남기고 정리 + Telegram 알림. PID 크기는 나이 순서가 아니므로(랩어라운드)
  `ps` 의 경과시간(`etimes`)으로 고른다.

### 배포 후 실서버 확인 (2026-09-10 23:5x KST, 미국장 개장 중)

```
$ curl -s http://158.180.81.25:8501/api/status
{"bot_status":"running","market_status":"US","heartbeat_age_sec":14.3,
 "heartbeat_source":"watchloop","session_active":true,"healthy":true}
```

- `market_status: US` — 서머타임 반영 (수정 전에는 `CLOSED`)
- `session_active: true`, `source: watchloop` — **매매 세션이 실제로 돌고 있다**
- `heartbeat_age` 는 `DCA_MONITOR_INTERVAL`(60초) 주기로 갱신 — 임계 300초 대비 5배 여유

> `/api/status` 에 `session_active` 와 `heartbeat_source` 를 노출한 이유:
> **"봇이 살아 있다"와 "봇이 매매 세션을 돌고 있다"는 전혀 다른 상태**인데
> 이걸 구분할 값이 없어서, 진단 중에 heartbeat 나이가 '늘어나는지 리셋되는지'를
> 반복 폴링해 추론해야 했다. 추론이 필요하면 그건 아직 관측이 아니다.

### v6.1 에서 발견한 전략 결함: 문제는 매수가 아니라 청산이었다

v6.0 의 검증 루프는 **노출도**만 봤다. 5개 시드를 돌려보니 전 국면에서 노출도가
22~25% 로 거의 같았고(폭락장만 10~16%), 그런데도 수익률은 상승장 +5%(B&H +38%),
폭락장 -28%(노출 13%) 였다. **노출 13% 로 -28% 를 잃으려면 같은 자리를 반복해서
손절당하고 있어야 한다.**

노출도 지표로는 이걸 구조적으로 볼 수 없다. 그래서 두 지표를 추가했다:

| 지표 | 정의 | 무엇을 잡는가 |
|------|------|---------------|
| **캡처율** | 수익률 / (평균노출 × B&H수익) | 트레일링이 승자를 조기에 털어내는가 |
| **회전손실** | 수익률 − (평균노출 × B&H수익), %p | 손절 휩쏘로 그냥 보유보다 더 잃는가 |

v6.0 설정(-7% / +5% / 3.5%)의 실측:

| 국면 | 노출도 | 수익률 | 동일노출 보유 | 캡처율 | 회전손실 |
|------|--------|--------|---------------|--------|----------|
| 강한 상승장 | 23% | +4.4% | +6.7% | 66% | −2.3%p |
| 완만한 상승 | 23% | −0.5% | +1.6% | **−34%** | −2.2%p |
| 횡보장 | 23% | −3.8% | −0.7% | — | −3.1%p |
| 조정장 | 21% | −8.9% | −4.5% | — | −4.4%p |
| 폭락장 | 14% | −16.3% | −7.5% | — | −8.8%p |

**전 국면에서 회전손실이 음수다.** 즉 이 전략은 "같은 평균 노출도로 그냥 들고
있는 것"보다 항상 손해였다. 완만한 상승장 캡처율 −34% 는 오른 만큼을 트레일링으로
전부 반납했다는 뜻이다.

원인은 명확하다. 일간 변동성이 1.1~3.5% 인 자산에 손절 −7% / 트레일링 활성 +5% /
하락폭 3.5% 는 **노이즈 밴드 안**이다. v3~v6 의 모든 개편이 매수 게이트만 건드리고
청산 규칙을 건드리지 않았기 때문에 이 결함이 네 버전을 살아남았다.

### 파라미터 스윕 결과 (80세션 × 5국면 × 5시드)

| 손절 / 활성 / 하락 | 강한상승 캡처 | 완만상승 캡처 | 폭락장 노출 | 판정 |
|--------------------|---------------|---------------|-------------|------|
| −7 / 5 / 3.5 (v6.0) | 66% | **−34%** | 14% | ❌ |
| −10 / 8 / 5 | 70% | 29% | 18% | ❌ |
| **−12 / 10 / 6 (v6.1)** | **72%** | **76%** | **20%** | ✅ |
| −12 / 12 / 8 | 67% | 51% | 21% | ✅ |
| −15 / 15 / 10 | 83% | 65% | 23% | ✅ |
| −20 / 15 / 10 | 83% | 65% | 24% | ✅ |

`-12 / 10 / 6` 을 채택했다. 전 게이트를 통과하는 가장 좁은 설정이고, 완만한
상승장 캡처율이 가장 높으며, 통과 설정 중 폭락장 노출이 가장 낮다.

> ⚠️ `atr_stop_max_pct` 를 함께 −12 로 넓히지 않으면
> `effective_stop_loss_pct()` 가 `max(-12, -10) = -10` 으로 되감아, **설정을
> 바꿔도 동작이 바뀌지 않는다.** 조용히 무시되는 설정을 남기지 말 것.

### 채택 후 실측 (3개 시드 패밀리 전부 통과)

| 국면 | 노출도 | 매도횟수 | 캡처율 | 회전손실 |
|------|--------|----------|--------|----------|
| 강한 상승장 | 26% | 2 (was 7) | 72~78% (was 66%) | −1.8~−2.1%p (was −2.3) |
| 완만한 상승 | 26% | 1 (was 7) | 58~76% (was −34%) | −0.4~−0.7%p (was −2.2) |
| 폭락장 | 20% | 6 (was 13) | — | −7.0~−10.1%p (was −8.8) |

휩쏘 매도가 절반 이하로 줄었다. 대가는 폭락장 노출 14%→20% 다 — 손절폭을 넓히면
개별 포지션이 더 깊게 물린다. 이건 튜닝이 아니라 **의식적인 교환**이다.

### 검증 루프의 한계 (다음에 고칠 것)

- **단일 종목 시뮬레이션이다.** 실제 봇은 US 12종목 / KR 21종목을 동시에 다룬다.
  종목당 노출 한도(25%)가 단일 종목 시뮬에서는 구속력이 있지만 실계좌에서는
  아니다. 멀티 티커 포트폴리오 모델이 다음 과제다.
- **ATR 동적 손절과 레버리지 스케일링을 모델링하지 않는다.** 실제 손절폭은
  `effective_stop_loss_pct()` 로 더 넓어진다. 스윕 결과는 보수적인 하한이다.
- **체결/슬리피지/수수료가 없다.** 회전손실 지표는 실제로는 더 나쁘다.

---

## v6.0 (2026-09-08) — 전면 개편: "게이트는 허가가 아니라 사이즈다"

### 무슨 일이 있었나

한국장·미국장 모두 **몇 달간 거래가 0건**이었다. 봇은 매일 정상 부팅했고, 하트비트를
찍었고, 세션을 열었고, 게이트를 평가했고, 전부 차단한 뒤 조용히 종료했다.
어느 단계에서도 예외가 발생하지 않았다. **정상 동작하는 것처럼 보이면서 아무 일도
하지 않는 것** — 이 프로젝트가 겪은 가장 비싼 실패 모드다.

원인은 하나가 아니라 다섯 개였고, 서로를 가려주고 있었다.

| # | 원인 | 영향 | 왜 아무도 몰랐나 |
|---|------|------|------------------|
| ① | `get_foreign_balance()`가 USD 통화 라인이 없을 때 `{'deposit': 0}`을 반환. v5.0 방어코드는 `'deposit' in foreign_bal`로 판정 → 키가 **존재**하므로 정상 분기 | US 전 종목 매일 "자금 부족" 차단 | 이 상황을 잡으려고 넣은 알림이 정확히 이 경로를 비껴감 |
| ② | `aggressive_dca`의 매수 판단이 **세션당 종목별 1회**, 개장 직후에만 실행. 장중 재평가 경로가 코드에 아예 없음 | 하루치 매매가 개장 한 틱으로 확정 | `blocked` 상태는 에러가 아니라 정상 종료로 로깅됨 |
| ③ | 추세 게이트 `현재가 > MA20` (KR은 `AND > MA5`) | 조정장이면 100% 차단 | "리스크 관리가 작동 중"으로 보임 |
| ④ | `check_portfolio_drawdown()`이 '고점 대비 낙폭'이 아니라 **'원가 대비 평가손실'**. 해제 조건 없음 | -7% 물리면 신규 매수 영구 차단 | 자기강화적 — 안 사니 원가 불변, 계속 -7% |
| ⑤ | 미국장 시간을 KST 23:30~06:00으로 하드코딩 (EST 기준) | 서머타임(1년의 약 2/3) 동안 개장 첫 1시간 상실 + 폐장 후 1시간 동안 닫힌 시장에 매도 주문 | 휴장일 개념도 없었음 |

### v4.0과 v5.0은 같은 버그였다

v4.0은 "무조건 사라"(게이트 전부 OFF) → 반도체 붕괴장에서 계속 물타기, 대손실.
v5.0/v5.1은 "게이트를 전부 복원" → 몇 달째 한 주도 못 삼.

정반대로 보이지만 **동일한 구조 결함**이다: 매수 판단이 **이진(통과/차단)** 이고,
그 판정을 **하루 한 번** 내린다. 임계값을 어느 쪽으로 밀든 "전부 사거나 / 전혀
안 사거나" 두 극단밖에 나오지 않는다. **튜닝으로 고칠 수 없는 문제였다.**

### v6.0 원칙

1. **게이트는 허가가 아니라 사이즈를 정한다.** 대부분의 신호는 0~1 점수로 환산해
   매수 수량에 곱한다. 나쁜 신호 = 적게 산다. (나쁜 신호 = 안 산다 → 폐기)
2. **판단은 장중 내내 반복된다.** `defer`는 "오늘은 끝"이 아니라 "지금은 아니다".
   `REEVAL_INTERVAL_SEC`(기본 300초)마다 모든 후보를 다시 평가한다.
3. **진짜 veto는 소수만. 모든 veto는 자동 해제 조건과 알림을 갖는다.**
   조용히 영구히 막히는 경로를 코드에 남기지 않는다.
4. **하락은 감점이 아니라 가점일 수 있다.** 단 '눌림목'과 '낙하칼날'을 구분한다:
   판단 기준은 `가격 < MA20`이 아니라 **`MA20 < MA60`(추세 구조)** 이다.
5. **조용한 실패는 실패로 간주한다.** 연속 무매수 세션을 추적해 등급별로 에스컬레이션.

### 핵심 신규 모듈

| 모듈 | 대체 대상 | 역할 |
|------|-----------|------|
| `modules/market_clock.py` | 하드코딩 시간표 | `zoneinfo` 기반 거래소 현지시각, DST·휴장일·조기폐장 반영. 세션 키는 **거래소 현지 영업일** 기준(KST 자정 리셋 버그 제거) |
| `modules/decision_engine.py` | 이진 게이트 8종 | 점수 기반 판단. `buy` / `defer` / `veto` 3상태 반환 |
| `modules/risk_state.py` | `check_portfolio_drawdown` 영구 래치 | 고점 대비 진짜 낙폭 + 사이즈 사다리(100→60→30→0%) + cool-off 자동 해제 |
| `modules/health_monitor.py` | `session_buy_count==0` 단발 워치독 | 세션 누적 기록, 연속 무매수 추적, 지배적 차단 사유 + 조치 힌트 |
| `scripts/verify_strategy_loop.py` | (없었음) | 5개 시장 국면 × N세션 시뮬레이션. 국면별 노출도/사이즈 단조성 검증. CI 게이트 |

### 점수 항목과 가중치 (`decision_engine._WEIGHTS`)

| 항목 | 가중치 | 의미 |
|------|--------|------|
| `regime` | 0.26 | **MA20 vs MA60** — 정배열이면 눌림목 매수, 역배열이면 축소. 가장 중요 |
| `trend` | 0.22 | 가격 vs MA5/MA20/MA60 위치 |
| `pullback` | 0.12 | 당일 저점 대비 반등폭 (낙하칼날 회피) |
| `sentiment` | 0.12 | AI 시장/섹터 감성 (HIGH 감점, LOW 가점) |
| `decline` | 0.10 | 연속 하락 폭 |
| `gap` | 0.08 | 갭 방향/크기 |
| `position` | 0.06 | 종목 노출 소진율 |
| `avg_down` | 0.04 | 평단 대비 위치 |

최종 점수 < `min_buy_score`(0.35) → `defer`. 이상이면 점수에 비례한 사이즈로 매수.
추가 배율: 레버리지 ETF ×0.7, 개장 30분 내 ×0.5, 낙폭 사다리 ×(1.0~0.0).

### 하드 veto 목록 (전부 자동 해제 조건 있음)

| veto | 해제 조건 |
|------|-----------|
| 주문가능금액 부족 | 현금 확보 시 다음 주기 즉시 |
| 금일 손절 종목 재매수 | 다음 영업일 |
| 세션 매수 횟수 한도 | 다음 세션 |
| 폐장 15분 전 | 다음 세션 |
| 계좌 낙폭 서킷브레이커 | cool-off 3영업일 또는 낙폭 절반 회복 |
| 일일 손실 한도 | 익일 자정 |
| 패닉 갭다운 (≤ -8%) | 익일, 또는 당일 반등으로 갭 개선 시 |
| AI 시장/섹터 CRASH | 다음 감성 조회(캐시 15분) |
| 상관그룹 신규 진입 한도 | 그룹 내 포지션 청산 시 (기보유 종목 추가매수는 막지 않음) |
| 종목 노출 한도 도달 | 가격 하락/일부 청산 시 |

### 리스크 파라미터 재조정 (검증 루프 근거)

`scripts/verify_strategy_loop.py`로 5국면 × 5시드 검증한 결과, 기존 손절 -4%는
일간 변동성(1~3.5%) 대비 **노이즈 밴드 안**이라 상승장에서도 승자를 계속 털어냈다.

| 설정 | 상승장 | 완만 | 횡보 | 조정 | 폭락 | 평균 |
|------|--------|------|------|------|------|------|
| -4% / 4% / 2.5% (기존) | +1.4% | -3.5% | -5.8% | -7.2% | -10.3% | -5.1% |
| **-7% / 5% / 3.5% (v6.0)** | **+3.1%** | -1.6% | **-3.1%** | -7.1% | -12.4% | **-4.2%** |

→ `stop_loss_pct: -7.0`, `trailing_stop_activation_pct: 5.0`, `trailing_stop_drop_pct: 3.5`

> ⚠️ 이 수치는 **합성 GBM 데이터** 기준이다. 갭/실적/팻테일이 없으므로 실제 폭락장
> 방어력은 표보다 나쁠 수 있다. 실데이터가 쌓이면 같은 스크립트로 재검증할 것.

---

## 프로젝트 철학 (v6.1)

**"멈춰 있는 전략은 안전한 것이 아니라 고장 난 것이다."**

> v5.x는 "잃지 않는다"를 최우선에 두었고, 그 결과 **아무것도 하지 않는 상태**에
> 도달했다. 그런데 아무것도 하지 않는 것은 리스크가 0인 상태가 아니다 —
> 기회비용은 실현되고, 고장은 감지되지 않으며, 계좌는 인플레이션만큼 줄어든다.
> 무엇보다 **"안 사는 것"이 리스크 관리처럼 보였기 때문에 몇 달간 발견되지 않았다.**

핵심 원칙:

1. **매수는 항상 가능해야 한다. 다만 크기가 달라진다.** 완전 차단은 명시적 사유와
   자동 해제 조건을 가진 소수의 veto에만 허용한다.
2. **뉴스는 무시하지 않는다.** 시장 전체 + 섹터 단위. 단 CRASH만 veto이고
   HIGH/LOW는 점수 가중이다 (LLM 한 번의 보수적 판단이 전 종목을 막지 않도록).
3. **매도(리스크 관리)는 엄격하게 유지한다.** 단 손절폭은 변동성에 맞춘다 —
   노이즈 안에 손절선을 두는 것은 엄격한 게 아니라 그냥 틀린 것이다.
4. **서킷브레이커는 반드시 꺼질 수 있어야 한다.** 해제 조건 없는 차단은
   서킷브레이커가 아니라 영구 정지다.
5. **한국장과 미국장은 다른 시장이다** (아래 "시장별 차별화" 참조).
6. **조용한 실패는 실패다.** 연속 무매수는 알림 대상이다.
7. **[v6.1] 관측되지 않는 고장은 고쳐지지 않는다.** 화면이 사실과 다르면 그 뒤의
   모든 판단은 추측이다. 이 프로젝트가 v3→v6 까지 네 번 "전면 개편"을 하고도
   증상이 남은 이유는 전략이 아니라 **계기판**이 고장나 있었기 때문이다.
   전략을 고치기 전에 계기판이 사실을 말하는지부터 확인하라.
8. **[v6.1] `buy` 판정은 반드시 1주 이상으로 이어진다.** 현금이 1주를 감당하는데
   0주를 내면 판단 계층이 거짓말을 하는 것이다. 과매수는 수량 0 이 아니라
   종목 노출 한도로 막는다.

---

## 시장별 차별화

| 항목 | 🇺🇸 US | 🇰🇷 KR |
|------|--------|--------|
| 개장 판정 | `America/New_York` 현지시각 (DST 자동) | `Asia/Seoul` 09:00~15:20 |
| 주문가능금액 | USD 외화예수금 → 0이면 원화 예수금 환산(통합증거금) | `ord_psbl_cash` 우선 |
| AI persona | `neutral` | `conservative` |
| 레버리지 | 3X ETF 허용 (사이즈 ×0.7) | 예탁금 기준 safe/risky 분리 |
| 개별주 손절 | 공통 | ETF보다 완화 (`KR_STOCK_STOP_LOSS_PCT`) |

## 전략 구조

### 매수 (Scored DCA — 내부 코드명은 `aggressive_dca` 유지)

```
장 개장 → 전 종목 1차 평가 → 점수 미달분은 'candidate' 로 보류
        ↓
장중 REEVAL_INTERVAL_SEC(300초)마다 전 후보 재평가 → 조건 충족 시 매수
        ↓
폐장 15분 전 신규 진입 중단 → 세션 기록 + 자가진단
```

판단은 전부 `modules/decision_engine.evaluate_buy()` 한 곳에서 이뤄지고,
**개장 루프와 장중 루프가 같은 함수를 호출한다** (`run_bot.job.evaluate_and_buy`).
v5.x의 최대 결함이 이 둘이 서로 다른 코드였고 후자가 아예 없었다는 점이므로,
**이 두 경로를 다시 분리하지 말 것.**

반환 3상태:

| 상태 | 의미 | 후속 |
|------|------|------|
| `buy` | 매수 실행 | `size_multiplier`만큼 수량 산정 후 주문 |
| `defer` | 점수 미달. **차단이 아님** | `status='candidate'` → 다음 주기 재평가 |
| `veto` | 명시적 차단 | 사유 기록 + 자동 해제 조건 대기 (역시 재평가 대상) |

> `status='blocked'`(그날 종결)은 v6.0에서 **폐기**됐다. 신규 매수 경로에서 종결
> 상태를 만들면 통합 테스트 `test_blocked_status_no_longer_terminal`이 실패한다.

### 물타기 (Averaging Down)

v6.0에서는 물타기가 별도 로직이 아니라 **같은 점수 판단의 결과**다.
보유 종목도 매 주기 재평가 대상이며, `avg_down` 점수 항목이 평단 대비 위치를
반영한다 (평단 -3%~-8% 구간은 가점, -8% 이하는 감점).

폭주 방지는 "금지"가 아니라 **한도**로 한다:

- 종목별 노출 상한 `max_position_pct`(기본 25%) 도달 시 하드 veto
- 세션당 매수 횟수 `max_buys_per_session`(기본 10회)
- 역배열 구조(MA20 < MA60)에서는 `regime` 점수가 크게 깎여 자연히 소액화
- 계좌 낙폭 사다리가 전체 사이즈에 곱해짐

### 매도 (리스크 관리)

| 규칙 | 조건 | 동작 |
|------|------|------|
| Stop Loss | 손익 <= **-7%** (v6.0 조정) | 즉시 전량 매도 |
| Trailing Stop | 고점 대비 **-3.5%** (활성: **+5%** 도달 후) | 전량 매도 |
| Breakeven Stop | +2.5%~+3% 도달 후 매수가+0.2%로 하락 | 전량 매도 |
| Trend-Collapse | MA20 하회 + 손실 구간 | 즉시 매도 |
| Partial TP | 트레일링 활성가 도달 | 50% 부분 익절 |
| ATR Dynamic | ATR 기반 동적 손절폭 | 변동성 적응 |
| 당일 재매수 금지 | 손절 발생 종목 | 익일까지 신규 진입 차단 (휩쏘 방지) |

**계좌 낙폭 사다리** (`modules/risk_state.py`) — 매도 규칙이 아니라 사이즈 규칙:

| 고점 대비 낙폭 | 신규 매수 사이즈 |
|----------------|------------------|
| 0 ~ 7% | 100% |
| 7 ~ 12% | 60% |
| 12 ~ 18% | 30% |
| 18% 이상 | 0% (cool-off 3영업일 또는 낙폭 절반 회복 시 자동 해제) |

---

## 코드 수정 규칙

### 🚫 절대 하지 말 것

1. **매수 판단을 다시 이진(통과/차단)으로 만들지 마라.** v4.0(전부 통과)과
   v5.0(전부 차단)은 같은 버그의 양극단이었다. 새 조건을 넣고 싶으면
   `decision_engine` 의 **점수 항목**으로 넣어라. veto 를 새로 추가하려면
   반드시 자동 해제 조건을 함께 설계하고 표에 기재할 것.
2. **하루 1회 판정으로 되돌리지 마라.** 개장 루프와 장중 루프는 같은
   `evaluate_and_buy()` 를 호출해야 한다. 둘을 분리하는 순간 v5.x 사고가 재발한다.
3. **해제 조건 없는 차단을 만들지 마라.** 그건 서킷브레이커가 아니라 영구 정지다.
   `check_portfolio_drawdown()`(원가 대비 평가손실)을 매수 차단에 다시 쓰지 말 것.
4. **시장 시간을 하드코딩하지 마라.** 반드시 `modules/market_clock` 을 경유한다.
   KST 고정 시간표는 서머타임에서 반드시 틀린다.
5. **`except: pass` 로 조용히 삼키지 마라.** 특히 잔고/시세/주문 경로. 과거 사고가
   반복적으로 이 패턴에서 났다 (2026-05-26, 2026-06-24, 2026-07, 2026-08).
6. **손절 기준을 "더 타이트하게" 되돌리지 마라.** v6.0 의 -7%/+5%/3.5% 조차
   일간 변동성(1.1~3.5%) 안이라 전 국면에서 "같은 노출로 그냥 보유"보다 손해였다
   (완만한 상승장 캡처율 **-34%**). 현재값은 -12%/+10%/6%.
   바꾸려면 `verify_strategy_loop.py` 스윕 결과를 첨부할 것.
7. **[v6.1] 로그 파일명에 날짜를 박지 마라.** `logging.FileHandler(f"...{today}.log")`
   는 프로세스가 살아 있는 동안 회전하지 않는다. 며칠 살아 있는 봇은 과거 파일에
   계속 쓰고, 화면은 다른 파일을 본다. 회전은 `TimedRotatingFileHandler` 가 한다.
8. **[v6.1] 화면이 로그 파일을 "추측"하게 하지 마라.** `sorted(glob(...))[-1]` 은
   대시보드 자기 로그를 고를 수 있다. 경로는 `modules.logger.BOT_LOG_FILE` 한 곳에서만
   정의한다.
9. **[v6.1] `pgrep` 결과를 "살아 있음"으로 표시하지 마라.** 멈춘 프로세스도 pgrep 에
   잡힌다. 생존은 heartbeat 파일의 **나이**로 판정한다.
10. **[v6.1] 정수화한 수량에 사이즈 배율을 곱하지 마라.** `int(1 * 0.5) = 0`.
   배율은 **금액에** 곱하고 그 다음 정수화한다.
11. **[v6.1] 감시 장치를 감시 대상 안에 넣지 마라.** `job()` 안의 자가진단은
   `job()` 이 안 도는 고장을 잡을 수 없다.
12. **[v6.1] 좀비 감시(heartbeat 나이)를 건드릴 때는 "정상적으로 오래 걸리는
   구간"을 먼저 나열하라.** 그 목록이 heartbeat 를 찍어야 할 지점이다.
   현재 덮여 있는 구간: `job-enter → session-prep → scan → sentiment →
   evaluate → watchloop`. 하나라도 빠지면 **정상 동작 중인 봇이 강제 종료된다.**
13. **[v6.1] `deploy.sh` 와 `auto_restart_bot.sh` 가 동시에 봇을 띄우지 않게
   하라.** 봇이 두 개면 같은 신호에 주문이 두 번 나간다. `database/.deploying`
   락과 `kill_duplicate_bots()` 를 제거하지 말 것.

### ✅ 해야 할 것

1. **`python scripts/verify_strategy_loop.py --seeds 5 --quiet` 통과 확인** —
   전략/청산/사이징을 건드렸다면 필수. 종료코드 0이어야 한다. 노출도 단조성,
   캡처율, 회전손실, 소액계좌 사이징 중 하나라도 깨지면 실패한다.
2. **`pytest tests/test_v60_strategy.py tests/test_v60_integration.py
   tests/test_v61_observability.py` 통과 확인** — 각각 판단·배선·관측 계층의
   회귀 테스트다.
3. **매수가 실행되는지, "왜 실행되지 않았는지"도 확인** — 세션 로그의
   `자가진단:` 줄과 `skipped_buy_reasons` 를 본다.
4. **연속 무매수 알림을 무시하지 마라** — 3세션이면 WARN, 7세션이면 CRITICAL이
   지배적 차단 사유와 조치 힌트를 함께 보낸다. 이 알림이 이번 사고를 몇 달이 아니라
   일주일 안에 잡았을 장치다.
5. **코드 수정 후 반드시** `python -c "import ast; ast.parse(open('run_bot.py', encoding='utf-8').read())"` 실행.

---

## 환경 설정

### .env 필수 값

```env
KIS_MOCK=False          # 실투자 필수
KIS_APP_KEY=...
KIS_APP_SECRET=...
KIS_CANO=...
KIS_ACNT_PRDT_CD=01
GEMINI_API_KEY=...       # 섹터 뉴스 veto가 Gemini에 의존하므로 사실상 필수
```

### user_config.json 핵심 설정 (v6.0)

```json
{
  "strategy": "aggressive_dca",
  "market_settings": {
    "us": { "persona": "neutral" },
    "kr": { "persona": "conservative" }
  },
  "aggressive_dca": {
    "min_buy_score": 0.35,
    "max_position_pct": 25.0,
    "reeval_interval_sec": 300,
    "sentiment_ttl_sec": 900,
    "panic_gap_down_threshold_pct": 8.0,
    "sector_news_veto_enabled": true
  },
  "risk_management": {
    "stop_loss_pct": -7.0,
    "trailing_stop_activation_pct": 5.0,
    "trailing_stop_drop_pct": 3.5,
    "correlation_cap_enabled": true,
    "losing_streak_enabled": true
  }
}
```

**v5.x에서 제거된 키:**

| 제거된 키 | 이유 |
|-----------|------|
| `skip_ai_check` / `skip_trend_filter` / `skip_correlation_check` | 이진 게이트 자체가 폐기됨. 이제 전부 점수 항목 |
| `risk_management.portfolio_drawdown_pct` | 원가 대비 평가손실 기반 영구 래치. `modules/risk_state.py`(고점 대비 + 자동 해제)로 대체 |
| `aggressive_dca.portfolio_drawdown_halt_pct` | 동상 |
| `aggressive_dca.instant_buy_on_open` | 장중 연속 재평가로 대체되어 의미 없음 |

새 파라미터:

| 키 | 기본값 | 의미 |
|----|--------|------|
| `min_buy_score` | 0.35 | 이 점수 미만은 `defer`. 낮추면 자주, 높이면 까다롭게 |
| `max_position_pct` | 25.0 | 종목당 총자산 대비 최대 노출(%). 물타기 폭주 방지 |
| `reeval_interval_sec` | 300 | 장중 재평가 주기(초) |
| `sentiment_ttl_sec` | 900 | AI 감성 세션 캐시 수명. 종목마다 LLM을 부르지 않기 위함 |

---

## 테스트 방법

```bash
# 1. 구문 검증
python -c "import ast; ast.parse(open('run_bot.py').read())"

# 2. 단위 + 통합 테스트
pytest tests/ -q
pytest tests/test_v60_strategy.py -v        # v6.0 5대 사고 원인 회귀
pytest tests/test_v60_integration.py -v     # job() 배선 검증
pytest tests/test_v61_observability.py -v   # v6.1 관측 계층 + 소액계좌 사이징

# 3. 전략 검증 루프 (전략/청산/사이징을 건드렸다면 필수)
#    시드 5개 평균으로 판정한다 — 단일 시드 통과는 운일 수 있다.
python scripts/verify_strategy_loop.py --sessions 80 --seed 42 --seeds 5 --quiet
python scripts/verify_strategy_loop.py --sessions 80 --seed 7  --seeds 5 --quiet

# 3-1. 청산 파라미터 스윕 (손절/트레일링을 바꿀 때)
python scripts/verify_strategy_loop.py --seeds 5 --quiet        --stop -12 --trail-act 10 --trail-drop 6

# 4. 모의투자 테스트 (선택)
KIS_MOCK=True python3 run_bot.py

# 5. 실투자 실행
python3 run_bot.py
```

---

## Git 커밋 규칙

- 모든 수정 후 자동 커밋/푸시
- 커밋 메시지 형식: `feat:`, `fix:`, `refactor:`, `docs:`
- 매수 게이트를 약화(끄기/완화)하는 변경은 반드시 별도 branch에서 작업 후 리뷰

---

## 아키텍처 개요

```
run_bot.py (메인 봇)
├── config.py (환경변수)
├── user_config.json (사용자 설정 — market_settings로 KR/US 차별화)
├── strategies/
│   ├── volatility_breakout.py (VBO 타겟 계산 — day/swing 전략용)
│   ├── technical.py (MA, 갭다운, 연속하락 계산)
│   └── adaptive_volatility.py (적응형 전략 — 미사용)
├── modules/
│   ├── logger.py ([v6.1] 자정 회전 + 프로세스별 파일 분리. 봇=trading.log, 대시보드=dashboard.log)
│   ├── market_clock.py ([v6.0] 유일한 시계. zoneinfo 기반 DST·휴장일·조기폐장)
│   ├── decision_engine.py ([v6.0] 점수 기반 매수 판단 — buy/defer/veto)
│   ├── risk_state.py ([v6.0] 고점 대비 낙폭 사다리 + cool-off 자동 해제)
│   ├── health_monitor.py ([v6.0] 세션 누적 기록 + 연속 무매수 에스컬레이션)
│   ├── kis_api.py (해외 주식 API)
│   ├── kis_domestic.py (국내 주식 API)
│   ├── kis_websocket.py (실시간 가격 스트리밍)
│   ├── multi_llm.py (AI 합의 — 시장 전반 + 섹터 특화 veto)
│   ├── sector_news.py ([v5.0 신규] 종목→섹터 매핑, 섹터 뉴스 쿼리)
│   ├── gemini_analyst.py (뉴스 수집 + 섹터 크래시 판별 — persona 무관 엄격 모드)
│   ├── auto_strategy.py (자동 전략 전환)
│   ├── portfolio_manager.py (동적 포트폴리오 생성 — 추세추종 복합 점수)
│   ├── telegram_notifier.py (알림)
│   └── trade_journal.py (거래 기록)
├── database/ (JSON 기반 데이터)
│   ├── trading.log        ([v6.1] 봇 로그 — 대시보드가 읽는 파일. 자정 회전)
│   ├── dashboard.log      ([v6.1] 대시보드 자기 로그 — 봇 로그와 섞이지 않는다)
│   ├── heartbeat.json     ([v6.1] 봇 생존 신호. 감시/화면이 이 파일의 나이를 본다)
│   └── strategy_health.json ([v6.0] 세션 누적 기록 — 연속 무매수 추적)
├── scripts/
│   └── verify_strategy_loop.py (전략 검증 루프 — CI 배포 게이트)
├── docs/
│   └── baseknowledge.md ([v5.0 신규] 퀀트투자 입문자용 용어/개념 설명)
└── web/ (FastAPI 대시보드)
```

## 루프 엔지니어링 (전략 검증)

이 프로젝트의 실패는 "전략이 틀렸다"가 아니라 **"틀린 걸 몇 달 동안 아무도 몰랐다"**
는 것이다. 그리고 v6.1 에서 밝혀졌듯, 몰랐던 이유는 **계기판이 거짓말을 하고
있었기 때문**이다. 따라서 검증은 5개 층으로 쌓는다. 각 층은 서로 다른 실패를 잡고,
**아래 층이 위 층을 대신할 수 없다.**

| 층 | 장치 | 잡는 실패 | 실행 시점 |
|----|------|-----------|-----------|
| 1 | `pytest tests/test_v60_strategy.py` | 판단 로직 회귀 (DST, 낙폭 래치, 게이트 오류) | 커밋 전 / CI |
| 2 | `pytest tests/test_v60_integration.py` | **배선** 회귀 (장중 재평가 경로 소실, 종결 상태 부활) | 커밋 전 / CI |
| 3 | `pytest tests/test_v61_observability.py` | **관측 계층** 회귀 (로그 파일 어긋남, 시계 분기, 좀비 오탐, 소액계좌 0주) | 커밋 전 / CI |
| 4 | `scripts/verify_strategy_loop.py` | "돌아는 가는데 아무것도 안 사는" 상태 + **청산 규칙 품질** | 커밋 전 / CI |
| 5 | `modules/health_monitor.py` + `_check_session_stall()` + heartbeat | 운영 중 조용한 정지 | 실시간 |

### 3층: 관측 계층 회귀 테스트 (v6.1 신규)

가장 늦게 만들어졌지만 **가장 먼저 돌아야 하는 층**이다. 1·2·4층이 아무리 통과해도
화면이 사실과 다르면 운영자는 오진하고, 오진한 상태에서 내리는 모든 결정은 추측이다.
2026-09-10 사고에서 세 계기판이 동시에 거짓말을 했고, 그래서 v6.0 의 완벽한 CI 통과가
아무 의미가 없었다.

검사 항목: 로그 파일명에 날짜가 박히지 않는가 · 자정 회전 핸들러가 붙어 있는가 ·
대시보드가 봇 로그 경로를 **추측하지 않고** 읽는가 · 시계가 `market_clock` 한 곳뿐인가 ·
서머타임 중 봇과 대시보드 판정이 일치하는가 · heartbeat 를 쓰고 그 나이로 좀비를
판정하는가 · 감시가 `job()` 바깥에 있는가 · 실계좌 잔고에서 매수 수량이 1주 이상인가.

### 4층: 전략 검증 루프

```bash
python scripts/verify_strategy_loop.py --sessions 80 --seed 42 --seeds 5 --quiet
```

**[0] 소액 계좌 사이징** — 실계좌 수치($330.64/12종목, ₩2,715,678/21종목)를 그대로
넣어 매수 수량이 0으로 죽지 않는지 본다. 국면 시뮬레이션은 자금 '규모'를 모델링하지
않으므로 이 결함을 **원리적으로** 잡을 수 없다. v6.0 이 전 국면 통과 상태로 배포되고도
실계좌에서 한 주도 못 산 이유가 이것이다.

**[1] 국면 시뮬레이션** — 5개 국면(강한 상승/완만한 상승/횡보/조정/폭락)을 합성
가격으로 만들고, 장중 재평가·손절·트레일링까지 포함해 세션을 돌린다.
**시드 5개 평균으로 판정한다** — 단일 시드 통과는 운일 수 있다.

검증 항목:

| 항목 | 잡는 회귀 |
|------|-----------|
| 평균 노출도 하한 | 상승 국면에서 자본이 놀고 있음 (v5.x) |
| 평균 노출도 상한 | 위험 국면에서 과다 노출 (v4.0) |
| 매수 0건 국면 없음 | 전략 완전 정지 |
| 사이즈 단조성 | 나쁜 국면일수록 작게 사는가 |
| **캡처율** (v6.1) | 트레일링이 승자를 조기에 털어내는가 |
| **회전손실** (v6.1) | 손절 휩쏘로 그냥 보유보다 더 잃는가 |

> **캡처율 = 수익률 / (평균노출 × B&H수익)**, **회전손실 = 수익률 − (평균노출 × B&H수익)**
>
> v6.0 은 노출도만 봤다. 그래서 전 국면 노출도 22~25% 로 통과하면서도 완만한
> 상승장 캡처율이 **−34%**(오른 만큼을 전부 반납) 인 것을 보지 못했다.
> **노출도는 "얼마나 실려 있나"만 알려주고 "실려 있는 동안 얼마나 지켰나"는
> 알려주지 않는다.** 매수 게이트만 보는 지표로는 청산 규칙의 결함이 영원히 안 보인다.

파라미터 튜닝도 이 루프로 한다. 손절/트레일링 값은 `--stop / --trail-act /
--trail-drop` 으로 스윕하고, 채택한 값은 `user_config.json` 에 기록한다.
루프는 그 설정을 **읽어서** 검증한다 — 값을 스크립트에 복사해 두면 운영 설정을
바꿔도 검증이 옛날 값으로 통과하는 "검증하지 않는 검증"이 된다.

기준선 (80세션 × 5시드, `-12% / +10% / 6%`):

| 국면 | 노출도 | 매수 | 매도 | 수익률 | 동일노출 보유 | 캡처율 | 회전손실 | B&H |
|------|--------|------|------|--------|---------------|--------|----------|-----|
| 강한 상승장 | 26% | 40 | 2 | +5.4% | +7.5% | 72% | −2.1%p | +29.2% |
| 완만한 상승 | 27% | 25 | 1 | +1.4% | +1.9% | 76% | −0.4%p | +7.0% |
| 횡보장 | 26% | 42 | 1 | −1.9% | −0.8% | — | −1.2%p | −3.0% |
| 조정장 | 25% | 52 | 2 | −7.4% | −5.3% | — | −2.1%p | −21.1% |
| 폭락장 | 20% | 142 | 6 | −17.7% | −10.8% | — | −7.0%p | −54.3% |

> 절대 수익률보다 **국면 간 상대 관계**와 **캡처율/회전손실**을 보라.
> 알려진 한계: 단일 종목 시뮬이라 종목 노출 한도(25%)가 구속력을 갖는다(실운용은
> 12~21종목). ATR 동적 손절·레버리지 스케일링·슬리피지·수수료는 모델링하지 않는다.
> 따라서 회전손실 수치는 **낙관적인 하한**이다. 멀티 티커 포트폴리오 모델이 다음 과제.

### 5층: 운영 중 자가진단 (실시간)

세 장치가 서로 다른 층위를 감시한다.

**(a) heartbeat — 루프가 도는가**
봇이 `database/heartbeat.json` 을 주기적으로 갱신한다.
`auto_restart_bot.sh` 는 그 파일의 **나이**를 보고, 장중 5분(장외 30분) 이상
멈춰 있으면 프로세스가 있어도 좀비로 보고 `pkill -9` 후 재기동한다.
대시보드는 `🟠 Stalled` 를 표시한다.

> `pgrep` 만 보는 감시는 2026-09-10 의 고장을 **원리적으로** 잡을 수 없다.
> 프로세스는 44분 내내 멀쩡히 존재했다.

**(b) 세션 미진입 감시 — 장이 열렸는데 매매를 시작했는가**
메인 루프의 `_check_session_stall()` 이 "개장 후 10분이 지났는데 이 세션에
`job()` 진입 기록이 없다"를 감시하고 즉시 Telegram 으로 알린다.

> `health_monitor` 는 `job()` **안에서** 세션이 끝날 때 돈다. `job()` 자체가
> 안 도는 최악의 고장이 정확히 그 사각지대였다. 감시는 감시 대상 바깥에 둔다.

**(c) health_monitor — 매매는 시작했는데 사고 있는가**
세션 종료 시 `database/strategy_health.json` 에 기록하고 즉시 판정한다.

| 연속 무매수 세션 | 등급 | 동작 |
|------------------|------|------|
| 0 | ok | 로그만 |
| 1~2 | info | 로그만 (하루 0건은 정상) |
| 3~6 | warn | Telegram 경고 + 지배적 차단 사유 |
| 7+ | critical | Telegram 긴급 + 사유 + **조치 힌트** |

예시 알림:

```
🚨 [US] 12세션 연속 매수 0건 — 전략이 사실상 정지 상태입니다.
· 지배적 차단 사유: 주문가능금액 부족 (현금 0 < 1주 512) (100.0%)
· 조치: USD 예수금이 0일 가능성이 큽니다. KIS 계좌의 외화 예수금 또는
  통합증거금(원화주문) 설정을 확인하세요.
```

### 그 외

- **주간 백테스트 + OPRO 자동 최적화** — `modules/backtest_runner.py`,
  `modules/opro_optimizer.py` (KR 장 마감 후 16:00 KST 자동 실행)
- **정기 원격 점검** — 대시보드/로그를 주기적으로 점검. `/loop` 로 설정
